import 'package:flutter/material.dart';

class SheetOnePage2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(''),
      ),
      body: Center(
        child: Text('第二个页面'),
      ),
    );
  }
}
